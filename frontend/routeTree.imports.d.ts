// (intentionally left blank)
// NOTE: previous attempts to add ambient module declarations here caused
// TypeScript `declare module` errors because relative specifiers are not
// allowed. The generated `routeTree.gen.ts` file was adjusted to include
// explicit `.tsx` extensions so the typechecker can resolve imports.
